<?php

/**
 * Implements hook_help().
 */
function custom_module_help($route_name) {
  switch ($route_name) {
    case 'help.page.custom_module':
      return t('A custom module for multiple purposes, including renaming uploaded images with UUID.');
  }
}
